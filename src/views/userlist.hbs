<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <ul>
        {{#each users}}
            <li>{{name}}</li>
        {{/each}}
        </ul>
    </body>
</html>